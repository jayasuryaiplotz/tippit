var Observable = require('FuseJS/Observable');

var accessToken;
var userId;
var phpFiatChain;


module.exports = {
    accessToken: accessToken,
    phpFiatChain:phpFiatChain,
    userId: userId,
};